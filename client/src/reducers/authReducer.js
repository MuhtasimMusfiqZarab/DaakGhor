//create the reducer and export it
export default function (state = {}, action) {
  switch (action.type) {
    // case value:

    //     break;

    default:
      // no need to change anything
      return state;
  }
}
